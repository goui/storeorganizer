<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="?attr/selectableItemBackground"
                android:paddingBottom="8dp"
                android:paddingTop="8dp">

    <TextView
        android:id="@+id/layout_simple_item_1tv_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:textSize="20sp"/>

    <ImageButton
        android:id="@+id/layout_simple_item_1tv_delete_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/delete"
        android:src="@drawable/ic_action_delete_black"/>

    <ImageButton
        android:id="@+id/layout_simple_item_1tv_edit_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@id/layout_simple_item_1tv_delete_button"
        android:contentDescription="@string/edit"
        android:src="@drawable/ic_action_edit_black"/>

</RelativeLayout>